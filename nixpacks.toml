[phases.setup]
nixPkgs = ["nodejs_20", "libreoffice", "noto-fonts-cjk-sans"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate && npx next build"]

[start]
cmd = "npm start"
