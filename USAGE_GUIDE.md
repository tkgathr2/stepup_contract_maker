# 使用ガイド

## 🚀 クイックスタート

### 1. 開発サーバーが起動していることを確認

```bash
npm run dev
```

ブラウザで以下のURLにアクセス:
```
http://localhost:3000
```

### 2. 書類を生成

1. **会社情報を入力**
   - 会社名: 例「株式会社サンプル」
   - 住所: 例「東京都渋谷区渋谷1-2-3」
   - 代表者名: 例「山田太郎」
   - 郵便番号: 例「123-4567」

2. **「書類を生成」ボタンをクリック**
   - 処理には10-30秒ほどかかります（初回はPuppeteerの起動に時間がかかります）

3. **ダウンロード**
   - 契約書（PDF / Word）
   - 送付状（PDF / Word）

## 📋 機能詳細

### 自動機能

#### 1. 日付の自動挿入
- 送付状の日付欄に今日の日付が自動的に挿入されます
- 形式: YYYY/MM/DD（例: 2026/01/07）

#### 2. 郵便番号の自動フォーマット
- 入力した郵便番号に「〒」が自動的に付加されます
- 例: 「123-4567」 → 「〒123-4567」

### 生成される書類

#### 1. 人材紹介契約書
- 会社名が契約書の冒頭と末尾に挿入
- 住所と代表者名が末尾の署名欄に挿入

#### 2. 送付状
- 日付が自動的に今日の日付で挿入（YYYY/MM/DD形式）
- 郵便番号（〒付き）、住所、会社名が宛先に挿入

### 出力形式

- **PDF**: 印刷や電子送付に最適
- **Word**: 後から編集可能

## 💡 Tips

### Puppeteerの初回起動が遅い場合

初回のPDF生成時は、Puppeteerの起動に30秒程度かかる場合があります。
2回目以降は高速化されます。

### 郵便番号の入力形式

郵便番号は以下の形式で入力してください:
- ハイフン付き: 「123-4567」（推奨）
- ハイフンなし: 「1234567」（自動的にフォーマットされます）

生成される書類では、自動的に「〒」が付加されます。

### 生成されたファイルの保存場所

生成されたファイルは以下に保存されます:
```
public/generated/
```

開発サーバーを再起動すると、これらのファイルは削除される場合があります。

## 🔧 トラブルシューティング

### エラー: "PDF生成に失敗しました"

**原因**: Puppeteerの起動エラー

**解決方法**:
```bash
npm install puppeteer --force
```

### エラー: "テンプレートファイルが見つかりません"

**原因**: テンプレートファイルが正しい場所にない

**解決方法**:
以下のファイルが存在することを確認:
- `templates/contract_template.docx`
- `templates/invoice_template.docx`

### 画面が真っ白

**原因**: JavaScriptエラーまたはビルドエラー

**解決方法**:
1. ブラウザのコンソールでエラーを確認
2. 開発サーバーを再起動
```bash
npm run dev
```

## 📞 サポート

問題が解決しない場合は、開発チームにお問い合わせください。

株式会社ステップアップ
