# デプロイ手順書

## 概要

このドキュメントは、契約書・送付状自動生成システムをWEBに公開するための手順をまとめたものです。

## デプロイ前のチェックリスト

- [x] ローカルで `npm run build` が成功する
- [x] ローカルで `npm start` で正常に動作する
- [x] テンプレートファイルがGitに含まれている
- [x] 必要なファイルが .gitignore で除外されていない
- [x] 環境変数が不要（MVP版）
- [x] nixpacks.toml でLibreOfficeがインストールされる設定

## 技術スタック

- **フロントエンド**: Next.js 16, React 19, TypeScript
- **スタイリング**: Tailwind CSS 4, shadcn/ui
- **PDF生成**: LibreOffice (via libreoffice-convert), Puppeteer
- **テンプレート処理**: docxtemplater, PizZip

## 重要な注意事項

### LibreOfficeの依存性

このアプリケーションは、DOCX → PDF 変換に **LibreOffice** を使用しています。そのため、デプロイ環境でLibreOfficeがインストールされている必要があります。

- **Railway**: LibreOfficeのインストールをサポート（nixpacks経由）✓ 推奨
- **Vercel**: サーバーレス環境のため、LibreOfficeが使用できない ✗ 非推奨

## Railway へのデプロイ（推奨）

### 1. Railwayアカウントの準備

1. [Railway](https://railway.app) にアクセス
2. GitHubアカウントでサインアップ/ログイン

### 2. プロジェクトのデプロイ

1. Railwayダッシュボードで「New Project」をクリック
2. 「Deploy from GitHub repo」を選択
3. このリポジトリを選択
4. 自動的にデプロイが開始されます

### 3. デプロイ設定の確認

Railwayは以下のファイルを自動的に検出します:

- `railway.toml`: ビルドとデプロイコマンド
- `nixpacks.toml`: LibreOfficeのインストール設定
- `package.json`: 依存関係とスクリプト

### 4. 環境変数（MVP版では不要）

現在のMVP版では環境変数は不要です。将来的に追加する場合:

- Railwayダッシュボード → Variables タブで設定

### 5. デプロイの確認

1. **ビルドログの確認**
   - Railwayダッシュボードで「Deployments」タブを開く
   - ビルドログを確認し、エラーがないことを確認
   - LibreOfficeが正しくインストールされているか確認

2. **URLの取得**
   - デプロイが成功すると、RailwayがURLを提供
   - 「Settings」タブ → 「Domains」で確認

3. **動作確認**
   - 提供されたURLにアクセス
   - 会社情報を入力
   - 書類を生成
   - PDFがダウンロードできることを確認

### 6. トラブルシューティング

#### LibreOfficeが見つからないエラー

```
Error: LibreOffice not found
```

**解決方法:**
- `nixpacks.toml` が正しく設定されているか確認
- Railwayのビルドログで `libreoffice` がインストールされているか確認

#### メモリ不足エラー

```
JavaScript heap out of memory
```

**解決方法:**
- Railwayの「Settings」→「Resources」でメモリを増やす
- 推奨: 512MB 以上

#### PDF生成が失敗する

**確認事項:**
1. テンプレートファイル（`templates/*.docx`）がデプロイされているか
2. `public/generated/` ディレクトリが書き込み可能か
3. Puppeteerが正常に動作しているか（Chromiumのインストール）

## Vercel へのデプロイ（非推奨）

⚠️ **重要**: VercelはサーバーレスFunction環境のため、LibreOfficeが使用できません。

現在の実装では **Vercel はサポートされていません**。

将来的にVercelでデプロイする場合、以下の変更が必要です:

1. **PDF生成方法の変更**
   - LibreOfficeの代わりに、クラウドベースのPDF変換サービスを使用
   - 例: CloudConvert API, DocRaptor, PDFShift など

2. **または、PDF生成をクライアント側で実行**
   - ブラウザ内でPDF生成ライブラリ（pdf-lib、jsPDF など）を使用
   - ただし、レイアウトの再現性が低下する可能性あり

## デプロイ後の確認項目

### 自動テストツール

プロジェクトルートに `test_browser_final.html` があります。これをブラウザで開くことで、本番環境の包括的なテストを実行できます。

```bash
# Windowsの場合
start test_browser_final.html

# macOS/Linuxの場合
open test_browser_final.html
```

**テスト内容:**
- 書類生成APIの動作確認
- 契約書PDF・送付状PDFのダウンロード確認
- ファイル名形式の確認（会社名入り）
- 郵便番号自動取得の確認
- PDF内容の文字化けチェック

### 手動テスト

1. **書類生成**
   - [ ] 会社情報を入力できる
   - [ ] 郵便番号が住所から自動検索される
   - [ ] 契約書PDFが生成される
   - [ ] 送付状PDFが生成される
   - [ ] 契約書DOCXがダウンロードできる
   - [ ] 送付状DOCXがダウンロードできる
   - [ ] PDFに文字化けがない（フォント埋め込み確認）

2. **プレビュー機能**
   - [ ] 契約書プレビューが表示される
   - [ ] 送付状プレビューが表示される
   - [ ] プレビュー切り替えボタンが動作する

3. **日付と郵便番号**
   - [ ] 契約書の日付が令和形式で表示される
   - [ ] 送付状の日付が令和形式で表示される
   - [ ] 送付状に相手先の郵便番号が表示される
   - [ ] 郵便番号が住所の上に表示される

4. **ファイル名**
   - [ ] 契約書ファイル名: `人材紹介契約書(会社名様).pdf`
   - [ ] 送付状ファイル名: `送付状(会社名様).pdf`

### パフォーマンステスト

1. **生成速度**
   - [ ] 書類生成が30秒以内に完了する

2. **レスポンス性**
   - [ ] ページ読み込みが3秒以内
   - [ ] ボタンクリックに即座に反応する

### セキュリティ確認

1. **入力検証**
   - [ ] 不正な入力（XSS、SQLインジェクションなど）が適切にフィルタリングされる

2. **ファイルアクセス**
   - [ ] 生成されたファイルが適切に保護されている
   - [ ] 他のユーザーのファイルにアクセスできない

## カスタムドメインの設定（オプション）

Railwayでカスタムドメインを設定する場合:

1. Railwayダッシュボード → 「Settings」→「Domains」
2. 「Custom Domain」をクリック
3. ドメインを入力（例: contract.example.com）
4. DNSレコードを設定:
   - CNAME レコードを Railway の提供する値に設定

## モニタリングとログ

### ログの確認

- Railwayダッシュボード → 「Deployments」→ ログを確認
- リアルタイムでログをストリーミング

### エラー追跡

- `logs/debug_latest.txt` にエラーログが記録される
- Railwayのログで確認可能

## バックアップとロールバック

### ロールバック

1. Railwayダッシュボード → 「Deployments」
2. 以前のデプロイメントを選択
3. 「Redeploy」をクリック

### バックアップ

- テンプレートファイルは Git で管理
- コードはGitHubで管理
- 生成されたPDFは一時的（永続的な保存は不要）

## パフォーマンス最適化

### 推奨設定

- **メモリ**: 512MB 以上
- **CPU**: 共有 CPU で十分
- **ディスク**: デフォルト（1GB）で十分

### キャッシュ戦略

- 静的ファイル（CSS、JS、画像）はCDNでキャッシュ
- APIレスポンスはキャッシュしない（毎回生成）

## スケーリング

### 水平スケーリング

- Railwayは自動的にトラフィックに応じてスケーリング
- 追加設定は不要

### コスト最適化

- 使用量に応じた課金
- 無料枠: $5/月
- 推奨プラン: Hobby ($5/月）または Pro ($20/月）

## サポートとヘルプ

### Railway サポート

- [Railway ドキュメント](https://docs.railway.app)
- [Discord コミュニティ](https://discord.gg/railway)

### プロジェクトサポート

- GitHub Issues でバグ報告
- プロジェクトドキュメントを参照

## まとめ

このアプリケーションは、RailwayへのデプロイでLibreOfficeが自動的にインストールされ、すぐに使用できます。デプロイ後は、上記の確認項目をテストして、正常に動作することを確認してください。

問題が発生した場合は、ログを確認し、必要に応じて設定を調整してください。
